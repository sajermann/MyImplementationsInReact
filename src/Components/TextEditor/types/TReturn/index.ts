export type TReturn = {
	text: string;
	startForSelection: number;
	endForSelection: number;
};

export type TReturnInfos = {
	selectedText: string;
	completeText: string;
	beforeText: string;
	afterText: string;
	selectionStart: number;
	selectionEnd: number;
};
